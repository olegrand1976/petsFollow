export GCP_PROJECT_ID="${GCP_PROJECT_ID:-premedica-prod-2025}"
export GCP_RUN_REGION="${GCP_RUN_REGION:-europe-west9}"
export AR_REGION="${AR_REGION:-europe-west1}"
export VPC_CONNECTOR="${VPC_CONNECTOR:-premedica-connector}"
export CLOUDSQL_INSTANCE="${CLOUDSQL_INSTANCE:-premedica-prod-2025:europe-west9:premedica-db-staging}"
export CUSTOM_DOMAIN="${CUSTOM_DOMAIN:-petsfollow.ll-it-sc.be}"
export API_CUSTOM_DOMAIN="${API_CUSTOM_DOMAIN:-api.petsfollow.ll-it-sc.be}"
export API_SERVICE="${API_SERVICE:-petsfollow-api}"
export FRONTEND_SERVICE="${FRONTEND_SERVICE:-petsfollow-nuxtjs}"
